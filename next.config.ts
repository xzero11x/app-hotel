import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: false, // Para evitar warnings de hidratación por extensiones del navegador
};

export default nextConfig;
